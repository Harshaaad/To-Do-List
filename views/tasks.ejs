<!DOCTYPE html>
<html>
<head>
  <title>To-Do List</title>
  <style>
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    
    .container {
      max-width: 400px;
      margin: 30px auto;
      background-color: #fff;
      box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
      padding: 20px;
    }
    
    h1 {
      text-align: center;
      margin-top: 0;
    }

    h2 {
      text-align: center;
      font-size: medium;
    }

    nav {
      background-color: #333;
      margin-bottom: 20px;
    }
    
    nav ul {
      list-style-type: none;
      padding: 0;
      margin: 0;
      display: flex;
    }
    
    nav li {
      margin-right: 10px;
      margin-top: 5px;
      margin-left: 5px;
    }
    
    nav a {
      text-decoration: none;
      color: #fff;
      padding: 5px 10px;
      border-radius: 4px;
    }
    
    nav a:hover {
      background-color: #555;
    }
    
    
    input[type="text"] {
      width: 100%;
      padding: 10px;
      box-sizing: border-box;
      border: 1px solid #ccc;
      border-radius: 4px;
      margin-bottom: 10px;
    }
    
    ul {
      list-style-type: none;
      padding: 0;
      margin: 0;
    }
    
    li {
      margin-bottom: 10px;
      display: flex;
      align-items: center;
    }

    .del {
      margin-left: 10px;
    }
    
  </style>
</head>
<body>
    <nav>
        <ul>
            <li><a href="/">Add Task</a></li>
            <li><a href="/tasks">Tasks</a></li>
        </ul>
    </nav>
  <div class="container">
    <h1>Tasks</h1>
    <% if(task.length > 0) { %>
    <ul>
        <% task.forEach((task) => { %>
            <li><%= task.taskId %>. <%= task.taskName %><form action="/del" method="POST"><input type="hidden" name="taskToDel" value="<%= task.taskId %>"><button class="del" type="submit">❌</button></form></li>
        <% }); %>     
    </ul>  
    <% } else { %>
      <h2>No Tasks..!</h2>
    <% } %>    
  </div>
</body>
</html>
